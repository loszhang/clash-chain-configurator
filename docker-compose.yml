version: '3.8'

services:
  clash-chain-configurator:
    build: .
    container_name: clash-chain-configurator
    ports:
      - "5000:5000"
    volumes:
      # 持久化配置文件
      - ./node_config.json:/app/node_config.json
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
