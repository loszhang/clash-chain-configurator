name: clash-chain-configurator

services:
  app:
    build: .
    container_name: clash-chain-configurator
    ports:
      - "5000:5000"
    volumes:
      # 持久化数据目录
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
